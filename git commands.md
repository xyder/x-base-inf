#### Clone a repo into a directory (*dir* can be relative):
  git clone \<url\> \<dir\>
#### Remotes management:
  git remote -v  
  git remote rm \<name\>  
  git remote add \<name\> \<url\>  
  git remote rename \<name\>
